import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#522566",
        secondary: "#7A3A8E",
        accent: "#AD74C3",
        light: "#EADFF0",
        soft: "#F8EDFB",
      },
    },
  },
};

export default config;